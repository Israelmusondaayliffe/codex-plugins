# Font setup for the coverword theme

CyberpunkReplica.ttf is not distributed in this public repository because
the local copy carries a personal-use license.

If you have a validly licensed copy, set CYBERPUNK_REPLICA_FONT_PATH to its
absolute path before running the theme generator.

The generator will use that file without copying it into the repository.
